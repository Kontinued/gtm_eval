# Copy this file to .env and fill in your key. .env is gitignored and never
# committed. The app auto-loads .env on startup (via python-dotenv).

GEMINI_API_KEY=

# Optional overrides:
# GEMINI_MODEL=gemini-2.5-flash
